# Issue Tracker

This repository uses **GitHub Issues** as its issue tracker.

When agent skills (like `to-spec` or `wayfinder`) need to read, create, or update issues, they should use the `gh` CLI (e.g., `gh issue list`, `gh issue view`, `gh issue create`).

PRs as a request surface: false
