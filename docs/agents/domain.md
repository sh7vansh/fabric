# Domain Docs

This repository uses a **single-context** layout for domain documentation.

- **`CONTEXT.md`** at the repository root holds the core domain glossary and architectural invariants.
- **`docs/adr/`** at the repository root holds Architectural Decision Records (ADRs).

When agents need to understand the domain or record new decisions, they must read and write to these central locations.
